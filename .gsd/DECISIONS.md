# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | App architecture | Single HTML file, no framework | Keeps deployment trivial — single file, any static server. Already established pattern. | No |
| D002 | M001 | arch | Page navigation | Show/hide full-screen divs within single HTML file | No routing library needed. Simple, fits single-file constraint. | No |
| D003 | M001 | library | Map rendering | Leaflet + OpenStreetMap tiles via CDN | Free, no API key, solid mobile performance. No vendor lock-in. | Yes — if richer maps needed |
| D004 | M001 | arch | Video storage | IndexedDB for video chunks and trip data | localStorage too small for binary blobs. IndexedDB handles large objects and has reasonable mobile quotas. | No |
| D005 | M001 | arch | Settings storage | localStorage for all settings | Simple key-value, already partially used. Settings are small strings/booleans. | No |
| D006 | M001 | pattern | Alert mode default | Quiet mode (no ambient narration) as default | Indian road density makes constant narration unusable. Verbose mode opt-in via settings. | No |
| D007 | M001 | arch | Video recording | MediaRecorder API with 1-minute chunks | Browser-native, no dependencies. 1-minute granularity balances incident capture vs storage management. | Yes — if chunk duration needs tuning |
| D008 | M001 | pattern | Trip boundary detection | GPS speed-based: start on movement, end after 3 min stopped | Simple, reliable, no external service needed. Uses existing Geolocation watchPosition. | Yes — if threshold needs tuning |
| D009 | M001 | pattern | Event tagging sources | Accelerometer (DeviceMotionEvent) + critical ADAS alerts | Both physical impacts and detected threats tagged on trip timeline. Comprehensive incident record. | No |
| D010 | M001 | pattern | Trip history navigation | Calendar view with date picking | Uber-style UX — intuitive, date-oriented browsing. | No |
| D011 | M001/S01 | pattern | Critical alert hook architecture for S01 boundary contract | Global `fireCriticalAlert(type, details)` function with `window._lastCriticalAlert` object for downstream consumption | Single-file architecture means no module exports. Global function + window property is the simplest hook pattern. Downstream slices (S04 video auto-lock, S05 trip event tagging) can read `window._lastCriticalAlert` or poll/check it. No event system needed — the alert fires synchronously in the render loop, and consumers check the state on their own cadence. | Yes |
| D012 | M001/S01 | pattern | Alert cooldown strategy to prevent blocking genuinely new threats | Per-object cooldown map keyed by `trackId + '_' + alertType` | The single `lastCriticalAlert` timestamp blocks ALL alert types when ANY alert fires. Keying by trackId + alertType means a crossing alert for pedestrian A doesn't block a TTC alert for vehicle B, and a crossing alert doesn't block a TTC alert even for the same object. This is critical for Indian road scenarios where multiple threats are simultaneous. | Yes |
| D013 | M001/S01 | pattern | fireCriticalAlert encapsulates cooldown management | fireCriticalAlert() handles per-object cooldown updates internally. Callers pass trackId in the details object; the function writes alertCooldowns[trackId + '_' + type]. | Centralizing cooldown management in fireCriticalAlert prevents callers from forgetting to update cooldowns, ensures consistent behavior across all 6 alert paths, and keeps the caller code clean — just pass type and details. | No |
| D014 | M001/S02 | pattern | Full-screen page z-index layering scheme | Footer at z-index 100, full-screen pages at 150, start-overlay at 200 | Pages must overlay the HUD content and footer but stay below the start/splash overlay. This three-tier stack leaves room for future overlays (modals, toasts) between layers without conflicts. | Yes — if more overlay layers are needed |
| D015 | M001/S03 | pattern | Settings boundary contract | getSetting/setSetting defined at top of script block, exposed on window | Downstream slices (S04, S05, S06) need a clean API to read settings like retentionMinutes. Defining at script top (before constants) ensures availability everywhere. Type coercion based on defaultValue type eliminates string-vs-number bugs. | No |
| D016 | M001/S03 | pattern | Settings page init-on-show pattern | initSettingsPage() called from showPage() via pageId check | Each full-screen page may need to sync its UI state from live variables when opened. Hooking into showPage() with a pageId guard is the standard pattern — future pages (gallery, trips) should follow the same convention. | No |
| D017 | M001/S05 | arch | IDB version bump strategy for adding dashcam_trips store | Single shared openVideoDatabase() bumped from v1→v2 with idempotent onupgradeneeded handler | Both VideoRecorder and TripLogger need the same database. Opening at different versions causes versionchange events that close connections. A single function at v2 with objectStoreNames.contains() guards ensures both stores exist without breaking existing v1 data. | No |
| D018 | M001/S05 | pattern | Accelerometer API preference for impact detection | Prefer event.acceleration (gravity-excluded), fall back to event.accelerationIncludingGravity with baseline offset | event.acceleration reads 0 at rest, making threshold comparison straightforward (impact > 2g). accelerationIncludingGravity has ~1g baseline, requiring offset. Not all devices support event.acceleration, so fallback is necessary. | Yes |
| D019 | M001/S05 | pattern | Gallery video playback approach | Inline video element within gallery card with toggle behavior | Avoids navigation or modal overlays. Click PLAY adds a video element to the card; click again removes it. Blob URLs tracked and revoked on video end and gallery close to prevent memory leaks. Simple and mobile-friendly. | Yes |
| D020 | M001/S06 | pattern | Video-to-event linking strategy | Client-side timestamp matching via getAll() on dashcam_videos store | Uses getAll() and JS-side filtering rather than IDB index range queries. Simpler code, adequate performance for the expected small dataset (a few dozen video chunks per session). If chunk count grows significantly, an IDB index on startTime would improve performance. | Yes |
| D021 | M001/S06 | pattern | Map event marker type | L.circleMarker with color-coded fill instead of icon markers | circleMarker renders as pure SVG — no external icon assets needed, consistent cyberpunk styling (red = impact, magenta = ADAS alert), smaller payload, and works offline. | No |
